#User function Template for python3

class Solution:
    def findWinner(self, n, A):
        # code here
        xor = 0
        for num in A:
            xor ^= num
        if xor == 0:
            return 1
        if n % 2 == 0:
            return 1
        else:
            return 2
